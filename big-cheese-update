#!/bin/bash

set -e
set -v

# Replace platform code of last installation.
rm -rf $PLATFORM_ROOT_LAST_INSTALL
cp -R $WORKSPACE $PLATFORM_ROOT_LAST_INSTALL

# Empty all caches.
drush "${SITE_LAST_INSTALL}" cc all

# Update database.
drush "${SITE_LAST_INSTALL}" updb -y
